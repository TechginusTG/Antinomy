import { Layout, Button } from "antd";

const Header = ({ className }) => {
    return (
        <Layout>
            <h2>ANTINOMY</h2>
            <Button>
                <span>Q</span>
            </Button>
        </Layout>
    );
};

export default Header;
